import { useEffect, useMemo, useState, type FormEvent, type ReactNode } from "react";
import { Plus, ShieldCheck } from "lucide-react";
import { operationsApi, type Vehicle, type VendorOption } from "@/api/operations";
import { VendorDrawer } from "@/components/vendors/VendorDrawer";
import { Badge, Busy, Empty, inputClass, Panel, PageIntro, primaryButtonClass, Problem, SearchField, StatCard, StatGrid, tableHeadClass, date } from "@/pages/Operations/operation-ui";
const expiring=(value?:string)=>Boolean(value&&new Date(value).getTime()<Date.now()+30*86400000);
export function VehiclesPage(){const [vehicles,setVehicles]=useState<Vehicle[]>([]),[vendors,setVendors]=useState<VendorOption[]>([]),[query,setQuery]=useState(""),[open,setOpen]=useState(false),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[error,setError]=useState("");const refresh=async()=>{setLoading(true);try{const [items,vendorItems]=await Promise.all([operationsApi.vehicles(),operationsApi.vendors()]);setVehicles(items);setVendors(vendorItems)}catch{setError("Vehicles could not be loaded.")}finally{setLoading(false)}};useEffect(()=>{void refresh()},[]);const vendor=(id:string)=>vendors.find(item=>item.id===id)?.legal_name??"Unknown vendor";const rows=useMemo(()=>vehicles.filter(item=>`${item.registration_number} ${item.make??""} ${item.model??""}`.toLowerCase().includes(query.toLowerCase())),[vehicles,query]);async function add(event:FormEvent<HTMLFormElement>){event.preventDefault();const data=new FormData(event.currentTarget);setSaving(true);setError("");try{await operationsApi.createVehicle({vendor_id:String(data.get("vendor_id")),registration_number:String(data.get("registration_number")),vehicle_type:String(data.get("vehicle_type")),make:String(data.get("make"))||undefined,model:String(data.get("model"))||undefined,rc_expiry:String(data.get("rc_expiry"))||undefined,insurance_expiry:String(data.get("insurance_expiry"))||undefined,status:"ACTIVE"});setOpen(false);await refresh()}catch{setError("Vehicle could not be saved. Registration numbers must be unique.")}finally{setSaving(false)}}return <><main className="space-y-5"><PageIntro eyebrow="Fleet control" title="Vehicles"><button onClick={()=>setOpen(true)} className={primaryButtonClass}><Plus className="h-4 w-4"/>Add vehicle</button></PageIntro><StatGrid><StatCard label="Registered fleet" value={vehicles.length}/><StatCard label="Active" value={vehicles.filter(item=>item.status==="ACTIVE").length}/><StatCard label="Attention due" value={vehicles.filter(item=>expiring(item.rc_expiry)||expiring(item.insurance_expiry)).length}/></StatGrid><Panel><div className="flex flex-col justify-between gap-3 border-b border-brand-border p-4 sm:flex-row"><p className="m-0 self-center text-sm font-semibold text-brand-forest">Fleet register</p><div className="sm:min-w-75"><SearchField value={query} onChange={setQuery} placeholder="Search registration or make"/></div></div>{loading?<Busy/>:error&&!rows.length?<Problem message={error}/>:!rows.length?<Empty title="No vehicles found" detail="Register a vehicle to begin tracking compliance."/>:<div className="hidden overflow-x-auto sm:block"><table className="w-full min-w-200 text-left text-sm"><thead className={tableHeadClass}><tr><th className="px-5 py-3">Vehicle</th><th>Vendor</th><th>RC expiry</th><th>Insurance expiry</th><th>Status</th></tr></thead><tbody>{rows.map(item=><tr key={item.id} className="border-t border-brand-border"><td className="px-5 py-4"><b>{item.registration_number}</b><p className="mb-0 mt-1 text-xs text-brand-muted">{[item.make,item.model,item.vehicle_type].filter(Boolean).join(" · ")}</p></td><td>{vendor(item.vendor_id)}</td><td>{date(item.rc_expiry)}</td><td>{date(item.insurance_expiry)}</td><td><Badge status={item.status}/></td></tr>)}</tbody></table></div>}</Panel></main><VendorDrawer open={open} onClose={()=>setOpen(false)} title="Register vehicle" subtitle="Attach it to an existing vendor profile"><form onSubmit={add} className="space-y-5">{error&&<p className="rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<Field label="Vendor"><select required name="vendor_id" className={inputClass}><option value="">Choose vendor</option>{vendors.map(item=><option key={item.id} value={item.id}>{item.legal_name}</option>)}</select></Field><Field label="Registration number"><input required name="registration_number" placeholder="MH 01 AB 1234" className={inputClass}/></Field><Field label="Vehicle type"><input required name="vehicle_type" placeholder="Truck, van, car…" className={inputClass}/></Field><Field label="Make"><input name="make" className={inputClass}/></Field><Field label="Model"><input name="model" className={inputClass}/></Field><Field label="RC expiry"><input name="rc_expiry" type="date" className={inputClass}/></Field><Field label="Insurance expiry"><input name="insurance_expiry" type="date" className={inputClass}/></Field><button disabled={saving} className={`w-full ${primaryButtonClass}`}><ShieldCheck className="h-4 w-4"/>{saving?"Saving…":"Save vehicle"}</button></form></VendorDrawer></>}
function Field({label,children}:{label:string;children:ReactNode}){return <label className="grid gap-1.5 text-sm font-semibold text-brand-text"><span>{label}</span>{children}</label>}
